# 내전 경험 우선 V2.3

이 문서는 이전 모델의 보관본입니다. 후속 모델은 `RATINGS-V3.md`를 참조하세요.

버전 `role-skill-v2.3.20260917`. 선수별 목표값/하한선/예외 감점은 없다. V2.2의 일반 솔랭 기준과 초기 역할별 근거는 그대로 보존하지만, 그 기준점으로 반복 복원하는 보정에는 종료 시점을 둔다.

## 경험에 따른 종료

경기 전 누적 내전 참가 G, 해당 포지션 참가 R에 대해:

`retention = min(clamp((30−G)/20, 0, 1), clamp((20−R)/12, 0, 1))`

- 전체 10경기까지 초기 계수 유지, 10→30경기에서 선형 감소. 30경기를 마친 후부터 모든 역할의 솔랭 복원 항은 0이다.
- 같은 역할은 8→20경기에서 더 빨리 감소할 수 있으며 20경기를 마친 후 해당 역할의 복원 항은 0이다.
- V2.2의 priorChange에 retention을 곱한다. 미배치의 근거 없는 중립값은 기존과 같이 복원하지 않는다. 과거 최고 티어 근거도 종료 대상이다.
- 포지션 미확정이나 상대 미연결 경기도 정상 참가로 집계된 경우 G에 포함한다. 미확정 역할은 R에 포함하지 않는다. 같은 시리즈 할인, 상대 다양성, 지표 품질은 신뢰도에만 사용하고 종료를 연장하지 않는다. 5분 미만 경기, 동일 gameId 중복, 동일 참가자 중복 집계는 기존처럼 제외한다.
- 10/30/8/20은 운영용 설계값이며 통계적으로 최적화된 경계가 아니다. 반복 리플레이가 참가 수를 늘리지 않도록 원본 경기에서 매번 계산한다.

## 전체 롤력과 새 역할

- V2.2의 초기 감점 환산값 `(S−B)`도 동일한 retention으로 줄인다. 역할 시작 시 내전 점수를 섞었다면 당시 등록 근거 비율도 곱해 이중 환산하지 않는다.
- 전체 30경기 이후의 전체 롤력은 실제 플레이한 포지션 점수의 √유효표본 가중평균이다. 환산값을 덧붙이지 않는다. 한 역할만 20경기 이상 한 경우에도 그 역할 환산은 0이다.
- 처음 맡는 역할은 `등록 역할 seed × 전체 retention + 내전 전체점수 × (1−전체 retention)`에서 시작한다. 이전에 비교 지표가 있는 경기가 전혀 없다면 등록 seed를 잠정값으로 유지한다. 비교 누락만으로 검증된 실력을 새로 만들어내지 않는다.
- 이미 플레이한 역할은 자기 경기 기록으로 계속 학습한다. 현재 선호 역할 변경이나 솔랭 새로고침이 동결된 과거 근거를 바꾸지는 않는다.
- 미경험 역할은 경기가 0이며 신뢰도 0/잠정 표시를 유지한다. 전체점수나 과거 최고 티어가 모든 역할의 실제 실력이라고 주장하지 않는다.
- 복원 보정 종료는 초기값의 수학적 영향까지 즉시 제거한다는 뜻이 아니다. 이전부터 누적 학습된 점수에는 초기 추정의 간접 영향이 남는다. UI도 복원 계수를 전체 점수의 솔랭 비중으로 표현하지 않는다.

## 맞상대 기대치와 연결 감사

- 상대 포지션 점수와 기여 지표 비교는 유지하되 기대치 `tanh(gap/1200)`에 들어가는 gap을 ±400점으로 제한한다. 낮은 점수가 훨씬 높은 상대에게 졌다는 이유만으로 과도한 가점을 받거나, 강자가 지표를 과도하게 압도해야만 점수를 유지하는 것을 줄인다. 기대 신호의 최대 절댓값은 약 0.322다. 이 한계도 운영용 설계값이다.
- 승패 보조 항과 지표/챔피언 가중치는 이번 패치에서 변경하지 않았다.
- 원본 참가자에서 아군/상대 역할이 유일한지 먼저 확인한다. 미연결 참가자도 역할 중복 검사에 포함한다. 상대 미연결, 상대 역할 미확정, 역할 중복을 따로 기록하며 자동 추정/계정 연결은 하지 않는다.
- 역할별 `comparisons`, 전체 `comparisonCount`, 경기별 `comparisonStatus`, `internalGamesBefore`, `soloRetention`을 노출한다. 비교 누락은 신뢰도를 낮추지만 솔랭 복원을 연장하지 않는다.

## 검증 및 한계

- 회귀 테스트 30개: 재계산 동일성/순서 독립/미래 경기 누출 없음, 참가 기준 종료, 미확정 30경기의 종료, 신규 역할 이전, 기대치 상한 대칭성, 누락 사유, 역할 점수 독립성 등.
- 전체 운영 37명/156경기 리플레이에서 30경기 이후 `priorChange === 0`, 기존 seed/계정/경기/시리즈 보존을 검증한다.
- 과거 비교에서는 개인 기여 기대 MAE 0.1457→0.1366, 팀 승패 Brier 0.2369→0.2401. 기여 오차는 줄었지만 승패 오차는 소폭 늘었다. 원본 역할 중복 검사를 강화하여 비교 표본도 1개 감소했다. 독립 검증 데이터가 아니며 실력 추정 정확도 향상을 보장하지 않는다.
- 2611130932135616의 63경기 중 맞상대·지표 비교 45경기, 본인 역할 미확정 3경기, 상대 계정/역할 때문에 비교 못 한 15경기는 그대로 감사 대상이다. 임의 매칭하지 않는다.
- 그래프는 새 계산식으로 재계산한 역사이며 당시 표시값과 다르다. 원본 시리즈 ratingSnapshot은 변경하지 않는다.

## 백업 / 실행 / 롤백

백업: `../backup/2026-09-16T22-18-04-371Z-before-internal-first-v23`. 이전 커밋 `fb262574eba21dfa59c4a39e741ab62f58e23ee1`, 원본 상태/경기, 배포 엔진/클라이언트, source.zip, SHA-256 및 변경표/비교 감사 결과 포함.

`node --test tools/rating-engine.test.mjs`

`node tools/evaluate-rating-v23.mjs ../backup/2026-09-16T22-18-04-371Z-before-internal-first-v23`

`node tools/verify-rating-integration.mjs ../backup/2026-09-16T22-18-04-371Z-before-internal-first-v23`

V2.3 코드 커밋을 revert해 재배포하고 인증된 `/api/ratings/recalculate`를 호출하면 V2.2로 돌아간다. ratingSeedV22의 스키마/값은 변경하지 않았다. 백업 app-state 전체 덮어쓰기는 이후 활동을 유실시킬 수 있으므로 하지 않는다.
