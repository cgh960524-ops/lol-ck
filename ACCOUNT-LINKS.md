# 카드에서 부캐 연결

- 플레이어 카드의 `+ 부캐 연결` → `아이디#태그` 입력 → 연결.
- 카드에서 열면 본계정 선택을 고정하고 해당 플레이어의 연결 목록만 표시한다.
- 기존 내전 데이터 섹션 진입점은 유지한다. 이곳에서는 본계정을 선택할 수 있다.
- 경기 기록이 없는 부캐도 Riot Account API로 계정 식별자(PUUID)를 확인해 연결한다.
- 부캐의 솔로랭크, 주/부포지션, 티어, 평가 시드는 가져오거나 덮어쓰지 않는다.
- 기존 `playAliases` 매핑으로 수집된 내전 기록을 같은 플레이어에게 귀속한다. 과거 미연결 기록이 합산되면 내전 기반 롤력은 다시 계산될 수 있다.
- 다른 활성 플레이어의 본계정/부캐 및 자신의 본계정 연결을 거절한다. 같은 PUUID 재요청은 중복을 만들지 않는다.
- 해제는 계정 연결만 제거한다. 원본 경기/시리즈는 삭제하지 않는다.

## 저장 및 운영

`POST /api/player-accounts` `{playerId, riotId}` 와 `DELETE` `{playerId, puuid}` 사용.
서버에서 계정 충돌을 검증하고 저장 성공 후 UI에 반영한다. 일반 `/api/app-state` 저장은 기존 서버의 `playAliases`를 유지하므로 오래 열린 화면이 새 연결을 덮어쓰지 않는다. 플레이어 보관 처리 시에는 기존처럼 연결을 비운다.

이 변경은 기존 웹사이트의 공유 관리 권한을 그대로 사용한다. Riot ID 조회는 계정의 존재 확인이며 소유자 인증이 아니다. 본인만 변경할 수 있는 권한은 향후 로그인/계정 소유 인증과 별도로 연결해야 한다. 기존 공유 계정 연결은 자동 이전하거나 정리하지 않는다. Riot API 키 만료/조회 실패 시 연결하지 않고 오류를 표시한다.

## 검증

- `node --test tools/account-links.test.mjs`: 입력, 중복/충돌, 시드 및 솔랭 정보 보존, 연결 해제, 내전 동일 귀속.
- `tools/verify-account-links.mjs <백업 디렉터리>`: 임시 로컬 저장소와 모의 Riot 응답만 사용한다. 실제 연결/해제, 오래된 화면 저장, 오류, 새로고침, 키보드 및 PC/390/320px 화면을 검증한다.
