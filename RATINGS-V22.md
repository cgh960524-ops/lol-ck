# 실력 기준점 보존 · V2.2

운영 버전: `role-skill-v2.2.20260917`. 선수 이름별 분기, 목표 점수, 영구 하한선은 없다.

## 변경 이유

V2.1은 1450보다 높은 기본 실력을 부포지션에 50%, 기타 포지션에 25%만 전달했다. 이 초기값으로 과거 전체를 재계산하면서 이미 경험이 있는 상위권 포지션과 전체 점수가 함께 압축됐다. 이 규칙을 제거한다.

## 계산

1. 기존 동결된 일반 솔랭 기준 `S`를 보존한다. 미배치는 1450 중립값이다. 수동 peak override와 일반 기준은 계속 구분한다.
2. 역할 기본값 `B`: 주포지션 S, 부포지션 S−35, 기타 S−80. 400~3200 범위. 과거 최고점 P는 근거가 기록된 역할에서만 `seed = B + 0.5 × max(0, P−B)`로 반영한다. 타 역할에 과거 최고점을 복제하지 않는다.
3. V2의 맞포지션 기여·승패 보조·동일 시리즈 독립성·챔피언 보정은 유지한다.
4. 독립된 솔랭/역할 이력 근거를 잃지 않도록 매 경기 기준 근거 보정 R을 추가한다.

   `R = (seed − 경기 전 역할점수) × learning/1200 × strength × 12/(12+effectiveWeight) × metricQuality × seriesIndependence`

   strength는 일반 솔랭 근거가 있으면 2, 미배치지만 해당 역할 이력이 있으면 1, 근거 없는 미배치는 0이다. 맞상대가 없거나 비교 지표가 없으면 R=0. effectiveWeight는 이전 경기들의 지표 품질과 같은 시리즈 중복 할인을 반영한 누적 표본이다. 데이터가 쌓일수록 R의 영향이 줄어든다. 점수가 seed보다 높으면 R은 음수이므로 상승/하락 모두에 작동한다. 최저점 제한이 아니다.
5. 최종 역할 변동은 개인 기여+승패 보조+R을 합쳐 기존 경기별 상한(초기 140, 이후 95)과 전체 범위(400~3200)를 적용한다. 표시한 성분 합계와 최종 변동은 반올림/상한 때문에 다를 수 있다.
6. 전체 롤력은 실제 플레이한 역할별 `역할점수 + S − B`의 √effectiveWeight 가중평균이다. 즉, 역할 변경에 부여한 초기 감점을 되돌려 기본 실력 기준으로 합친다. 내전에서 실제로 학습한 역할 간 차이는 보존된다. 미경험 역할은 평균에서 제외한다. 기록이 없으면 주포지션 seed를 사용한다.
7. 팀 편성과 예상 승률에는 전체 점수가 아니라 실제 배정된 역할점수를 사용한다. 전체 점수보다 높은 부포지션도 허용한다.
8. 미경험·소수 상대는 여전히 잠정 점수다. 0경기 역할은 신뢰도 0, 경기 부족/상대 다양성 부족/낮은 근거 수준 표시를 유지한다.

## 제약

- 35/80, prior strength 2/1, 유효 표본 12 및 기존 지표 가중치는 운영용 설계값이며 실력의 객관적 정답이나 Riot MMR이 아니다.
- 동결된 등록 근거가 부정확하거나 오래됐다면 점수에도 영향이 있다. 단순 티어 새로고침은 과거 전체의 기준을 바꾸지 않는다. 날짜가 있는 새로운 솔랭 근거를 반영하는 기능은 별도 작업이다.
- 이번 변경은 과거 전체를 새 알고리즘으로 재계산한다. 그래프도 재계산된 역사이며 당시 표시되던 점수와 같지 않다. 기존 원시 경기/시리즈 스냅샷은 수정하지 않는다.
- 156경기 비교 리플레이에서 팀 승패 Brier는 0.2448→0.2369, 개인 기여 기대 오차 MAE는 0.1303→0.1457이었다. 전자는 개선됐지만 후자는 악화됐다. 개발에 사용한 기록이며 등록 근거가 과거 경기 이후의 정보일 수도 있으므로 독립 검증이나 정확도 향상의 증거로 주장하지 않는다. 이후 경기 검증이 필요하다.

## 마이그레이션 / 롤백

- ratingSeedV2와 ratingSeedV21을 보존하고 ratingSeedV22에 신규 프로필을 동결한다. 서버는 오래된 클라이언트의 PUT에도 이 세 근거를 보존한다.
- 백업: `../backup/2026-09-16T21-43-35-550Z-before-skill-baseline-v22` (운영 상태, 156경기, 배포 엔진, 소스 ZIP, SHA-256).
- `rating-v22-comparison.json`, `롤력-변경표.md`에 전원 변경 결과를 저장했다.
- 코드 롤백은 V2.2 커밋을 revert해 배포한 뒤 `/api/ratings/recalculate`로 V2.1 재계산한다. 이전 seeds가 보존되므로 새로 발생한 경기/시리즈를 지우지 않고 롤백할 수 있다. 백업 app-state 전체 덮어쓰기는 이후 활동을 유실시킬 수 있으므로 하지 않는다.

## 검증

`node --test tools/rating-engine.test.mjs`

`node tools/evaluate-rating-v22.mjs ../backup/2026-09-16T21-43-35-550Z-before-skill-baseline-v22`

`node tools/verify-rating-integration.mjs ../backup/2026-09-16T21-43-35-550Z-before-skill-baseline-v22`
