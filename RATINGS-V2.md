# 포지션 실력 V2 · 2026-09-17

목적은 승리 보상을 쌓는 것이 아니라 선수의 **현재 역할별 기여 수준을 추정**하는 것이다.
이 점수는 Riot MMR이나 객관적으로 검증된 절대 실력이 아니다.

## 운영 구조

- 브라우저·서버·Discord 모두 rating-engine.js를 사용한다.
- 전체 롤력은 실제 플레이한 역할 점수의 유효 표본 제곱근 가중 평균이다. 수동 전체 하한은 유지한다.
- 팀 구성에는 해당 배치 역할 점수를 사용한다. 부포지션도 전체 점수보다 높을 수 있다.
- 등록 시 기준점·주/부포지션·기존 역할 사전정보를 ratingSeedV2에 고정한다. 티어 갱신이나 역할 선택으로 과거 점수를 다시 쓰지 않는다.
- 솔랭 미배치는 고정 중립값 1450으로 시작한다. 알려진 솔랭/최고티어 정보는 기존 basePower 기준으로 시작한다.
- 확정 포지션만 학습한다. 역할 미확정 경기는 전적은 집계하지만 실력 학습에서 제외한다.
- 기존 데이터의 계정 별칭·과거 수동 역할 보정·세트별 roleOverrides를 유지한다.
- 서버는 클라이언트가 보낸 파생 점수를 신뢰하지 않고 원본 경기로 다시 계산한다. POST /api/ratings/recalculate는 기존 업로더 인증을 요구하며 알림을 보내지 않는다.

## 경기별 계산

맞상대는 같은 경기 반대 팀의 확정된 동일 포지션 1명이다. 중복/미확정이면 임의 상대를 선택하지 않는다.

1. 성장, 골드 대비 피해, 킬관여, 참여를 고려한 생존, 분당 시야, 탱킹, 제어, 아군 보호, 오브젝트·포탑 피해를 역할별 가중치로 비교한다.
2. 챔피언-포지션별 과거 표본을 전체 역할 평균으로 수축해 보정한다. 이후 경기의 지표는 과거 평가에 사용하지 않는다. 팀 골드 격차는 일부 지표에서 제한적으로 보정한다.
3. 관측 기여 신호와 tanh((내 역할 점수 - 상대 역할 점수) / 1200)의 차이가 기여 잔차다.
4. 개인 변화 = 학습률 × 기여 잔차 × 지표 가용도 × 상대 근거 보정 × 같은 방향 기여 가속 × 시리즈 반복 보정.
5. 학습률은 역할 0~2경기 180, 3~7경기 145, 8~19경기 115, 이후 90. 장기 공백에는 최대 1.3배까지 허용한다.
6. 최근 5경기 중 같은 방향으로 뚜렷한 기여 잔차가 3회면 1.2배, 4회 이상이면 1.4배. 승패에는 이 배수를 적용하지 않는다.
7. 승패 보조 = (초기 24, 이후 16) × (승패 - 경기 전 기대 승률). 불완전한 참가자 연결은 1/4로 줄인다.
8. 같은 시리즈의 n번째 후속 세트는 1 / sqrt(1 + 0.5n)로 줄여 반복 상대를 독립적인 새 증거로 과대평가하지 않는다.
9. 합계 변화는 초기 경기당 ±140, 이후 ±95 이내. 역할 점수 범위 400~3200. POG·다중킬은 별도 중복 보너스를 주지 않는다.

누락된 고급지표는 0으로 간주하지 않는다. 가용 지표만 비교하고 근거를 낮춘다. 신뢰도는 표본 수·상대 다양성·가용도에 대한 휴리스틱이며 통계적 정확도 확률이나 신뢰구간이 아니다.

## 화면 및 기록

- 플레이어 지표: 전체 점수, 역할 5개 점수/경기 수/근거 수준, 최근 변동 이유.
- 그래프: 전체/역할 선택. 과거 기록은 V2 재계산이며 당시 표시된 V1 점수가 아니다.
- 신규 시리즈: 생성 시 ratingSnapshot에 버전·전체/역할 점수·신뢰도를 기록.
- ratingHistory는 최근 120경기 상세만 전송한다. 전체 원본 경기는 계속 재계산한다.
- 기존 시리즈 승패, POG, 정산, 계정 링크, 게임 ID는 이 패치로 수정하지 않는다.

## 검증

node --test tools/rating-engine.test.mjs
node tools/verify-rating-integration.mjs <백업 폴더>
node tools/evaluate-power-v2.mjs <백업 폴더> --report

마지막 명령은 읽기 전용 비교 보고서를 백업 폴더에 생성한다. 현재 저장된 티어·별칭·역할 보정으로 재구성한 결과이므로 독립적인 미래 성능 검증이 아니다. 최근 50경기는 개발 중 확인한 표본으로, 향후 실제 팀 밸런스 검증을 대체할 수 없다.

기존 대비 점수 척도와 분포가 바뀐다. 임의로 선수 이름별 목표 점수에 맞추지 않는다. 향후 성능 검증 시 역할별 승리 예측 편향·연속 기대초과/미달·초기점수 민감도를 함께 확인할 것.

## 백업 및 롤백

패치 직전 커밋: 9696b038c66c2920c9b031fa9d4b79cecf805298
외부 백업 폴더: ../backup/2026-09-16T20-51-42-666Z-before-role-power-v2
내용: 운영 client.js, 소스 전체, 선수/시리즈 상태, 원본 156경기, SHA-256 manifest.

1. 현재 상태를 다시 백업한다.
2. V2 패치 커밋을 git revert하여 배포한다. 후속 변경이 있다면 충돌을 검토한다. 원본 경기 DB를 예전 스냅샷으로 덮어쓰지 않는다.
3. V1 페이지를 열어 최신 원본 경기로 재계산·저장한다. 웹/Discord 점수 일치를 확인한다.
4. 시점 전체 복원은 그 이후 신규 경기/등록을 잃을 수 있으므로 별도 명시적 요청 없이는 하지 않는다.

등록 기준점과 V2 파생 필드는 V1에서 사용하지 않는다. 후속 재적용 시 기존 ratingSeedV2의 의도적 유지/재설정 여부를 검토한다.
