# V3.1 — 미경험·소수 표본 포지션 전이 보정

후속 서폿 지표 보정은 `RATINGS-V32.md`를 참조하세요. 이 문서의 전이 보정은 유지됩니다.

`observed-skill-v3.1.20260917`. V3 지표 추정, 전체 점수, 확정 주·부포지션은 유지하고 다른 포지션으로의 과도한 실력 전이를 제한한다. 모든 잠정 선수를 감점하는 패치가 아니다.

## 기준과 계산

- 등록 주·부포지션은 `ratingSeedV22.primary/secondary`의 고정 정보를 사용한다. 현재 37명은 설정값과 고정 정보가 일치함을 확인했다. 역할 선택 UI를 바꾼 것만으로 과거 점수가 재작성되거나 보정이 우회되지 않는다.
- 다른 포지션의 최초 기준 A는 `min(V3 해당 역할 시작점, 그 시점 전체 롤력)`이다. 미경험 역할은 최종 상태에서 초기화한다. 이미 관측한 역할의 A는 최초 관측 시점으로 고정한다.
- 전이 감액 D는 `clamp(A × .12, 150, 300)`, 보수적 시작값 B는 `clamp(A − D, 400, 3200)`이다. 12%, 150~300은 운영 가정이며 통계적으로 확인된 숙련 손실량이라고 주장하지 않는다.
- 관측 반영률 W는 `min(1, 유효 비교 경기/8, 유효 시리즈/3, 유효 상대 수/3)`다. 모두 충족해야 1이다. 상대 역할/계정이 확인되고 지표 품질 65% 이상인 경기만 센다. 한 BO5를 다섯 시리즈로 세지 않는다. 누락 경기로 보정을 해제하지 않는다.
- V3의 관측 기반 역할 점수를 R이라 할 때 적용 목표는 `min(R, B + W × max(0, R − B))`다. 관측상 낮은 점수를 초기값 쪽으로 억지로 끌어올리지 않는다. W=1이면 목표는 R이므로 주·부 외 역할도 전체보다 높아질 수 있다.
- 첫 5경기 ±140, 이후 ±95의 기존 이동 한도를 적용 점수에도 유지한다. 검증 완료 시 목표 감액은 0이나, 이동 한도 때문에 적용 점수의 정착이 몇 경기 늦을 수 있다. 지표가 부족한 경기는 보정 해제나 점수 이동을 진행하지 않는다.

## 두 값의 역할

`evidenceRating`은 기존 V3의 관측 기반 역할 추정값이다. 전체 롤력 집계와 상대 난도 학습은 이 값을 사용한다. 보수적인 전이 가정을 실제 관측 실력 저하로 다시 학습시켜 다른 선수와 주·부포지션 점수까지 연쇄 하락시키지 않는다.

`rating`은 전이 보정을 거친 실제 배치 포지션 점수다. 웹의 팀 구성, 포지션 고정, 라인업 점수, 예측 입력, Discord `/롤력`에 동일하게 사용한다. 따라서 화면만 낮추는 변경이 아니다. `expected`는 경기 전 적용 점수에 따른 팀 예측이며 V3와 달라질 수 있다. 학습용 상대 기준은 `opponentEvidencePower`, 표시용 상대 점수는 `opponentPower`에 구분한다.

기존 V3의 전체 점수는 관측 실력의 요약으로 유지한다. 보정된 배치 점수의 평균으로 전체까지 중복 감점하지 않는다. 등록 주·부포지션이 실제 숙련을 보장하지는 않으며, 이번 범위에서는 사용자가 유지 요청한 해당 역할 추정을 그대로 보존한다.

## 표시와 감사

포지션 카드에 `포지션 전이 보정 −N점`, `관측 반영 N%`를 표시한다. 최근 관측 추정은 보정 전 지표 추정이므로 적용 점수와 다를 수 있다. 그래프와 변동량은 적용 점수 기준이다. `calibrationChange`를 따로 저장하여 근거 축적에 따른 감액 해제와 원래 수행 지표 변화를 구분한다. 관측 반영률은 실력의 정확도 확률이 아니다.

선수명 예외, 솔랭 복원, 수동 하한은 추가하지 않았다. V3의 등록 기준 감쇠와 고급지표 계산은 그대로다. 기준이 보수적이라는 이유로 실제로 못하는 포지션이라고 단정하지 않는다. 무경험 역할을 낮추면 예상보다 잘하는 유저가 저비용 배치될 위험도 있으므로 실제 역할 경기가 들어오면 해당 증거로 보정을 해제한다.

## 검증과 롤백

- 37명 전체 점수 및 등록 주·부 74개 역할 점수 동일.
- 기존 잠정이 아니던 역할 41개 점수 동일.
- 모든 역할의 보정 전 V3 값이 이전 운영 역할 점수와 동일함을 검사.
- 활성 선수의 71개 미경험/근거 부족 역할에만 하향 보정 적용.
- 원본 경기·시리즈·계정 식별·고정 등록 기준 보존. 중복 실행 결정성, 순서 독립성, 1시리즈 반복, 누락 데이터, 소수 표본 상승 제한, 검증 후 제한 해제, 주·부 유지 및 전체/포지션 그래프 연동 테스트.

백업: `../backup/2026-09-16T23-32-36-828Z-before-off-role-v31/`. 변경표는 `잠정-포지션-변경표.md`. V3 코드 기준은 커밋 `7499cc0`과 백업 `source.zip`이다. 이 패치 커밋을 되돌려 배포하고 현재 경기로 재계산하면 V3로 복귀한다. 새 경기 손실을 막기 위해 과거 운영 상태 전체를 덮어쓰지 않는다.

```
node --test tools/rating-engine.test.mjs tools/rating-v3.test.mjs tools/rating-v31.test.mjs
node tools/evaluate-rating-v31.mjs ../backup/2026-09-16T23-32-36-828Z-before-off-role-v31
node tools/generate-static-assets.mjs .
node tools/verify-rating-integration.mjs ../backup/2026-09-16T23-32-36-828Z-before-off-role-v31
```
