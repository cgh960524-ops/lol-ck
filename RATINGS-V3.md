# 관측 실력 V3

버전: `observed-skill-v3.20260917`. V2.3의 승패/기대 대비 잔차 적립을 대체한다. 특정 닉네임, 목표 1200점, 수동 하한선, 승률 보너스는 없다. 원본 경기, 시리즈, 챔피언 통계와 계정 연결은 변경하지 않는다.

## 의도와 한계

현재 포지션에서 반복적으로 관측된 수행 수준을 추정하고 현재 점수를 그 추정 수준에 접근시킨다. 잘한 경기의 보상을 무한히 누적하는 모델이 아니다. 경기 결과를 뒤집어도 다른 지표가 같으면 점수는 같다. POG도 중복 가산하지 않는다.

최종 통계만으로 오더, 자립적 판단, 라인 지원량, 시야의 실제 가치, 챔피언 숙련 전체를 식별할 수 없다. 팀 골드와 챔피언 보정도 인과적 기여도를 완전히 분리하지 못한다. 승패 예측 개선은 실력의 정답을 찾았다는 증명이 아니다. 솔랭 티어에 대응하는 공식 점수도 아니다.

## 고정 기준

`rating-reference.js`는 최초 79경기(2026-08-31까지)의 포지션·챔피언 지표를 집계한 고정 산출물이다. 나머지 77경기는 기준 생성에 사용하지 않았다. 같은 시리즈가 양쪽으로 나뉘지 않는지 생성 도구가 검사한다. 시리즈 미등록 경기의 클러스터는 개별 경기 ID다.

중심 1500, 지표 1단위당 650점은 운영 척도로 선택한 설계값이며 최적 추정치라고 주장하지 않는다. 요청마다 현재 선수 평균으로 다시 맞추지 않는다. 새 경기로 기준을 자동 재학습하지 않는다. 변경은 별도의 버전·검증·마이그레이션이 필요하다. 과거 리플레이에서도 이 고정 기준을 사용하므로 기준 생성 구간 안의 그래프는 당시 정보만으로 계산된 예측이 아니다.

## 경기 관측

실제 확정 포지션만 사용한다. 아군/상대 역할 중복, 상대 계정 미연결, 포지션 미확정이면 해당 포지션을 학습하지 않는다. 누락 필드는 0으로 대체하지 않으며 유효 지표 가중치 합이 65% 미만이면 학습을 보류한다. 참가·승패·KDA 통계는 그대로 집계한다.

- 성장: 분당 골드와 CS의 로그 변환.
- 효율: 획득 골드 대비 챔피언 피해.
- 교전: 팀 처치 관여율. 생존은 참여율을 함께 사용한다.
- 시야: 분당 시야. 탱킹: 분당 받은 피해·경감과 참여.
- 제어: 분당 CC. 보호: 아군 회복·보호막. 오브젝트: 분당 오브젝트·포탑 피해.
- 킬·어시·딜·받은 피해 등 하나만으로 캐리 능력이라고 단정하지 않는다.

포지션별 가중치(합계 1):

| 포지션 | 성장 | 효율 | 교전 | 생존 | 시야 | 탱킹 | 제어 | 보호 | 오브젝트 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 탑 | .30 | .20 | .10 | .08 | .03 | .12 | .08 | .02 | .07 |
| 정글 | .30 | .13 | .14 | .05 | .10 | .05 | .07 | .01 | .15 |
| 미드 | .27 | .28 | .18 | .08 | .06 | .03 | .05 | .02 | .03 |
| 원딜 | .30 | .34 | .17 | .10 | .03 | 0 | .02 | 0 | .04 |
| 서폿 | 0 | .04 | .22 | .05 | .23 | .12 | .15 | .16 | .03 |

챔피언 기준은 포지션 평균과의 차이를 `n/(n+20)`으로 축소하고 로그 단위 ±0.25로 제한한다. 표본 없는 챔피언은 차이 보정을 하지 않는다. 챔피언 수 자체에 점수를 주지 않는다.

팀 환경은 비교 선수 둘을 각각 제외한 나머지 아군 4명의 골드 비율을 사용한다. 양쪽 모두 4명이고 골드가 유효할 때만 반영한다. 로그 비율을 ±0.8로 제한하고 성장·오브젝트에 20%만 적용한다. 자신의 골드를 그대로 팀 이득으로 다시 빼지 않지만, 자신의 팀 기여가 동료 골드에 미치는 간접 영향까지 분리할 수는 없다.

각 지표를 고정 역할 평균/표준편차(최소 .18)로 표준화한다. 개인 표준 점수는 ±2.5, 상대와의 표준 점수 차이는 ±3으로 제한한다. 역할 가중 평균으로 A(개인 수행), D(실제 맞상대 차이)를 구한다.

```
개인 기준 = 1500 + 650 × A
상대 기준 = 경기 전 상대 포지션 점수 + 650 × D
난도 계수 = .65 × (.5 + .5 × 상대 근거 수준)
난도 보정 = clamp((상대 기준 − 개인 기준) × 난도 계수, −400, +400)
경기 관측값 = clamp(개인 기준 + 난도 보정, 650, 2850)
```

상대 점수는 여전히 맥락에 사용되므로 잘못된 상대 추정의 간접 영향이 완전히 0은 아니다. 다만 상한 있는 보정과 고정 개인 기준을 함께 사용한다. 모든 시작점에 300점을 더해도 충분한 동일 관측 뒤에는 그 300점이 영구 보존되지 않는 것을 테스트한다. 강한 상대를 만났거나 승리했다는 이유로 별도의 점수를 지급하지 않는다.

## 최근 추정과 이동

포지션별 최근 유효 12시리즈를 사용한다. 한 시리즈의 세트 관측값을 품질 가중 평균하고, 시리즈마다 평균 품질 한 표를 부여한다. 최근성 반감기는 6시리즈다. 시리즈 관측값은 최근 중앙값 ±450으로 윈저화하여 한 번의 대승/대패가 평균을 지배하지 않게 한다. 같은 상대만 반복하면 독립 근거가 충분하다고 표시하지 않는다.

등록 기준의 직접 비중은 `min(V2.3 경험 잔존계수, max(0, 1 − 보관된 유효 관측 수/12))`다. 전체 30경기 또는 포지션 20경기면 0이다. 지표 누락이 솔랭 복원 기간을 연장하지 않는다. 관측 없는 역할은 기존 등록/내전 전이 추정치를 잠정으로 보관하되 별도 비교 근거를 부여하지 않는다.

초기에는 등록 기준과 최근 추정을 혼합하고, 경험이 쌓이면 최근 추정만 목표로 한다. 현재 점수를 목표로 이동하되 첫 포지션 5경기는 경기당 ±140, 이후 ±95로 제한한다. 한 경기 관측값과 최근 추정값은 획득 점수가 아니다. 비교 불가 경기에 승패 보상도 주지 않는다.

전체 점수는 실제 관측된 포지션의 √유효표본 가중 요약이다. 초기 V2.3 역할 환산은 경험에 따라 종료된다. 팀 구성은 전체가 아닌 배치 포지션 점수를 사용한다. 역할이 전체 점수보다 높거나 낮을 수 있다.

## 표시·검증

포지션 카드에 최근 추정 수준/유효 비교/독립 시리즈를 표시한다. 최근 변동 설명에 경기 관측값과 최근 추정값을 분리한다. 그래프는 V3로 재구성된 추이임을 표시한다. 역할 8경기 미만, 상대 3명 미만, 관측 시리즈 3개 미만, 근거 수준 .35 미만 중 하나라도 해당하면 잠정이다. 근거 수준은 정확도 확률이 아니다.

후반 평가에는 연결된 10명과 양쪽 모델의 경기 전 예측이 모두 있는 74경기/25클러스터를 사용한다. 예측 오차 Brier는 V2.3 약 .2375, V3 약 .2309다. 클러스터 부트스트랩 차이의 95% 구간은 0을 포함하므로 통계적으로 확정된 개선이 아니다. 등록 정보가 해당 과거 경기보다 늦게 기록됐을 가능성이 있어 완전한 전향 검증도 아니다. 이번 개발 중 같은 후반 구간을 재확인했으므로 최종 점수의 독립적인 출시 후 검증을 대체하지 않는다.

전체/역할별 변경표를 저장한다. 하위 두 선수만 보지 않고 상위권·희소 표본·미연결 경기까지 확인한다. Xiao Mao 전체 점수 하락 등 큰 변동은 숨기거나 수동 하한으로 막지 않는다. 출시 후 실제 대진 경험과 추가 경기로 재검증해야 한다.

## 재현과 롤백

백업: `../backup/2026-09-16T23-02-31-036Z-before-observed-skill-v3/` (원본 운영 상태, 경기, 코드 archive, SHA-256 manifest).

V2.3 알고리즘은 `tools/rating-engine-v23.js`와 이전 커밋 `59b5faf`에 보존한다. 모델 코드 변경 커밋을 되돌려 배포하고 인증된 재계산을 실행하면 현재 원본 경기에서 이전 모델을 재생할 수 있다. 신규 경기 손실을 막기 위해 과거 app-state 전체를 운영에 덮어쓰지 않는다. 완전히 같은 과거 숫자의 감사에는 백업 JSON을 사용한다.

```
node --test tools/rating-engine.test.mjs tools/rating-v3.test.mjs
node tools/evaluate-rating-v3.mjs ../backup/2026-09-16T23-02-31-036Z-before-observed-skill-v3
node tools/generate-static-assets.mjs .
node tools/verify-rating-integration.mjs ../backup/2026-09-16T23-02-31-036Z-before-observed-skill-v3
```

변경되지 않는 고정 기준의 재현이 필요할 때만 `tools/build-rating-reference.mjs`에 위 백업을 전달한다. 운영 요청마다 실행하지 않는다.
