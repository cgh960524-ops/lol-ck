# 주·부포 우선 대안 생성

롤력 산출·포지션 점수·승률 예측 모델은 변경하지 않는다. 대안 생성의 포지션 배정과 후보 순서만 바꾼다.

1. 수동 포지션 고정을 최우선으로 지킨다. 같은 역할에 3명 이상 고정하거나 입력이 충돌하면 자동 해제하지 않고 생성 실패를 알린다.
2. 가능한 팀 분할 전체에서 정글·서폿의 등록 주·부포 외 배치 수를 최소화한다. 대안 다양성을 위해 불필요한 정글·서폿 예외를 추가하지 않는다. 양 팀의 네 자리를 주·부포로 채울 수 없으면 최소한의 예외를 허용하고 화면에 표시한다.
3. 동일한 후보 안에서는 참가자 전체 롤력이 낮을수록 주·부포 밖으로 보내는 비용을 더 크게 둔다. 주포 우선, 부포 다음, 나머지 포지션 순으로 고려한다. 동점 선수는 동일한 가중치를 받고 특정 닉네임 예외는 없다.
4. 선호 배치 비용과 기존 승률·맞라인 격차 비용을 함께 비교한다. 낮은 선수부터 무조건 자리를 잠그는 탐욕 배정이 아니라, 포지션 충돌과 팀 균형을 같이 평가한다. 낮은 선수의 주포를 항상 보장하는 규칙은 아니다.
5. 팀마다 가능한 역할 순열을 검토하고 정글·서폿 예외가 최소인 후보 중 선호 비용이 낮은 최대 24개를 보존한다. 양 팀 조합을 비교한 후 팀 분할별 상위 3개를 남기고, 팀 구성 다양성을 우선하여 최대 5개 대안을 고른다. 제한된 후보 탐색으로 절대 최적해를 보장하지 않는다.

주·부포는 현재 선수 레코드의 `role`, `secondary`를 사용한다. 과거 자동 등록값이 남아 있다면 현재 등록값에 따라 판단하며, 이 패치가 선수의 주·부포나 계정 정보를 자동으로 바꾸지는 않는다.

주·부포 합계와 정글·서폿 주·부포 배치 수를 결과에 표시한다. 기존 ‘남은 인원 서폿 배치’ 표기는 제거하고 주포·부포·예외를 구분한다. 직접 편집과 포지션 교환 기능은 유지한다. 기존에 저장된 시리즈나 대안표를 소급 재배치하지 않는다.

검증: 포지션 고정, 인원 부족 예외, 낮은 롤력 보호, 부포 인정, 선수/역할 중복 방지, 입력 순서 재현성, 불변 원본 테스트. 최근 3개 시리즈 명단으로 각 5개 대안을 생성해 정글·서폿 4/4 주·부포 및 예측 승률 약 46~53%를 확인했다. 이는 해당 명단의 회귀 점검이며 예측 정확도 검증이 아니다.
